import { NutritionistsTableFilterPipe } from './nutritionists-table-filter.pipe';

describe('NutritionistsTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NutritionistsTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
